import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  bkg2: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#5C3753',
  },
});
export default styles;
